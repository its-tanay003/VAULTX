# ── Week 4 additions to .env.local ──────────────────────────────────────────

# Claude / Anthropic (AI validation engine)
ANTHROPIC_API_KEY=sk-ant-YOUR_KEY_HERE

# Internal secret for API route auth (generate with: openssl rand -hex 32)
VAULT_INTERNAL_SECRET=your-random-32-char-secret-here


# ── Integration checklist ─────────────────────────────────────────────────────

# 1. Add to app/actions/submissions.ts (at end of createSubmission, before redirect):
#
#    import { triggerAIValidation } from "./ai-validation";
#    triggerAIValidation(submission.id).catch(console.error);

# 2. Run migration 002_fuzzy_search.sql in Supabase SQL Editor

# 3. Test the pipeline:
#    GET /api/ai/validate-submission  → should return health JSON
#    POST /api/ai/validate-submission { submission_id: "uuid" }
#      with header x-vault-secret: your-secret

# ── Token cost estimate ───────────────────────────────────────────────────────
# Stage 3 only fires when Stage 2 finds candidates (similarity > 0.35)
# Estimated: ~800 tokens per validated submission (input + output)
# Claude Sonnet 4.6 pricing: ~$0.003 per submission at scale
# For a demo with 50 submissions: ~$0.15 total — effectively free
