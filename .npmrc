# legacy-peer-deps removed — all packages now declare React 19 peer dep support.
# lucide-react upgraded to ^1.x (React 19 support + brand icons removed upstream)
# next-themes upgraded to ^0.4.x (React 19 support)
